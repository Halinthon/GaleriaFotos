<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <!-- Android 12 y menor -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/Theme.SimpleGallery">

        <!-- Pantalla principal: álbumes -->
        <activity android:name=".AlbumsActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Pantalla de fotos dentro de un álbum -->
        <activity android:name=".PhotosActivity" android:exported="false"/>

        <!-- Visor de foto individual con zoom -->
        <activity android:name=".ViewerActivity" android:exported="false"
            android:theme="@style/Theme.SimpleGallery.Black"/>

        <!-- Reproductor de video -->
        <activity android:name=".VideoActivity" android:exported="false"
            android:theme="@style/Theme.SimpleGallery.Black"
            android:screenOrientation="sensorLandscape"/>

        <!-- Necesario para compartir archivos con otras apps -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

    </application>
</manifest>
